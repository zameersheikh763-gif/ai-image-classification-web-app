# ai-image-classification-web-app
A Flask-based AI web application that classifies uploaded images using TensorFlow and MobileNetV2. The system allows users to upload an image through a simple HTML interface and returns real-time prediction results.
